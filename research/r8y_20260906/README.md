# R8y ensemble-size diagnostic

Only requested TabICL ensemble count changes on two frozen contexts: W's45-column baseline and X's47-column pair06 RR (usage slope + posterior rim). The same three historical folds, three seeds, dated labels, model checkpoint, normalization, precision, input order and exact-vector ties remain unchanged. No test/confirmation data or new source is used.

The requested grid was16/32/64/128/256. CPU-only inspection of actual train/query ensemble views found64/128/256 identical for every context/fold/seed: the generator caps at45 or47 feature permutations. These settings therefore share one evaluation, with explicit requested→evaluated mapping. Eighteen unique tasks remain. Runtime records requested and effective counts and requires the observed cap; it does not describe256 requests as256 actual models.

Six n32 tasks must exactly replay saved X raw/canonical predictions, scores and complete input/tie/constructor audits before interpretation. Summaries compare pair versus baseline at each evaluated size and each size versus32 within context, paired by model seed and historical fold. This is a development diagnostic with no promotion. Source-vintage and original-prospect-universe limitations remain.

The unchanged verified four-worker scheduler retains every full raw result as an immutable task file and provides compact per-fold dashboard rows. Frozen files cover code, plan, source package, references and the capacity proof. Essential fixture tests check the six-reference gate and six audit corruptions without estimator fitting; server CPU preflight verifies the exact two input designs in all three folds. Limits are2 hours,140 GiB and22 CPU-equivalents, with network disabled and only results writable.
