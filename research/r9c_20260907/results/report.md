R9c verification passed: 459/459 immutable task records, all 3 full reference replays, all 58 frozen file hashes and the complete recomputed summary match. No model or held-out evaluation was run during verification.

The queue completed in 358.538 seconds (76.81 tasks/minute), with no errors. It covers 35 single new-data columns and three whole-family matched-control groups and remains a pre-2019 development diagnostic, with no model promotion. The added-width, correlated-permutation and retrospective source limitations in the registration still apply.

All original task files remain on the server. 3 deterministic gzip chunks preserve their exact bytes (75,531,420 original bytes; 17,509,628 compressed bytes); every record passed round-trip comparison. Each chunk is below 40 MB. task_archive_manifest.json records original file, task ID, byte length, SHA-256, chunk and line, plus compressed and uncompressed chunk hashes. To restore, decompress a chunk and remove exactly one final LF from each record line.

The source package remains model_eligible=False under its separate bounded diagnostic admission. Eight GP/shooting representations are flagged; no source certification or held-out claim follows this study. Full numerical diagnostics are in matched_summary.json. Verification deliberately performs no selection for the next study.
