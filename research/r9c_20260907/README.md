R9c is a bounded diagnostic of 35 college player-game and team-context columns. It uses only the approved public `candidate_inputs.csv` projection (SHA256 `98f485709f136b5e4f716b6e15c877649fc48a7bb80fe38e9d9876317bebf596`): PID, draft year and 35 numeric features. The private 539-column augmented input is never copied or mounted. A separate diagnostic admission records this authorization; the original source package’s `model_eligible=False` status remains unchanged. This is not global source certification or model promotion.

The baseline follows the prespecified rule: among completed A/B deletions with positive changes versus full X in every fold mean and every model-seed mean, take the highest development mean; otherwise use full X. Exact ties use lexical source/id order. The frozen result is B `drop5_001`, removing `ctx_base_fta`, `ctx_base_ftm` and `ctx_base_mid_made` from X, leaving 44 predictors. The rule, full selection result and source summary/verification hashes are registered. No benchmark or confirmation results inform this decision.

There are 459 fits: three exact selected-baseline replays; 35 single features × real/three permuted arms × three model seeds (420); and whole player-game15, team20 and all35 families × real/three jointly permuted arms × three seeds (36). Every single uses the same added `slot_043`. Within each family/fold, real/control additions have identical columns and width. Permutations move entire selected vectors within training/query role, draft cohort and exact missingness patterns. They preserve per-player masks, cohort vector distributions and within-family structure; they may break dependence with the baseline. Sparse strata may be immovable. These are paired diagnostics, not significance tests.

The 1,458-row source projection is joined by exact PID and matching draft year. All 1,428 existing study PIDs are retained in their original population. New coverage never removes a row or fills a missing value with zero. Each development fold takes only its existing training/query PIDs. Added-field eligibility requires at least five observed training rows and two distinct training values; query data does not select fields. Before any fits, CPU preflight requires all 35 registered fields to pass in every fold, or fails closed for a registration revision without score feedback.

The source audit establishes strict pre-draft game dates, exact lineage/source/annual-row hashes, group quarantine and stable pre-2015 values when later candidate sources are removed. It reports 670 player-game and 624 team joins in the full pool, with no source-year gap greater than one. Retrospective publication/revision timing, complete schedules, credited-event discrepancies, DNP/minutes and prospect-universe limitations remain. Eight GP/shooting fields are algebraic representations of observations already present in the source41 family, including fields removed from the selected baseline; any gain from them remains a representation result. Other player rates reflect source-subset recomputation, and team-context fields provide different events. All eight are flagged in the plan and summary.

The fixed TabICL32 model, checkpoint, precision, normalization, training-only baseline selector, calendar labels, canonical SHA256(pid) row order and exact-identical-input ties are unchanged. Neither PID nor actual draft order enters model inputs. All three selected-baseline raw predictions, canonical predictions, scores and full audits must replay exactly before interpretation. Results distinguish matched real-minus-shuffled gains from actual change against the baseline; extra column width alone can alter the model. No source certification, automatic promotion or held-out evaluation follows this study.

All files and references are frozen before the first fit. Essential fixtures verify the exact public schema, left-join population, training-only eligibility, common single slot, family masks/joint-vector counts, source immutability and reference/structure tamper rejection. CPU preflight checks all 152 new designs in each of the three original pre-2019 development folds. The verified four-worker scheduler runs with two-hour, 140 GiB and 22 CPU limits, no weight reuse and no network, vault, confirmation or 2019+ input mounts. Complete task records stay immutable on the server. Fetch compact proofs/summary locally; later publication can use lossless task chunks.
