# R8z broader exploratory complement screen

Selection is frozen from W only: every statistic with positive mean real-minus-shuffle and positive mean real-minus-baseline, yielding19 fields. All171 unordered pairs are enumerated; the10 pairs completed in X are excluded. The161 new pairs use the unchanged RR/RP/PR/joint-PP protocol, three shuffle seeds and three model seeds, for4,830 new pair fits. W baseline/all19 singles supply60 exact references; X pair06 RR supplies three exact references for the47-column path. Total:4,893 fits. No Y result, new source, test or confirmation feedback selects this queue.

The unchanged source-only41 + dated-consensus4 backbone, TabICL32 settings, calendar label cutoffs, training-only eligibility, canonical pid ordering and exact-vector prediction ties remain fixed. Pairs retain47 columns and alphabetical slots041/042. Conditional controls preserve individual masks/cohort marginals; joint PP also preserves pair structure within joint-mask strata. This is exploratory model usability testing, not statistical significance, and there is no automatic promotion. Source-vintage, original-prospect-universe and existing input-quality limitations remain.

All63 references must exactly reproduce raw/canonical predictions, scores and audits before any new pair is interpreted. The frozen X worker replays pair06; the frozen W worker replays the baseline/singles. The pair implementation is inherited from X with only registration counts, identifiers, reference dispatch and immutable plan/reference caching generalized. No model settings change. Essential checks cover selection/counts/exclusions, reference contracts/tamper failures, server input/label/id/column equality, all19 fields' training eligibility, and representative RR/RP/PR/PP designs.

The benchmark-verified four-worker immediate-refill scheduler preserves complete results in remote immutable tasks/*.json plus an fsynced append log. Compact progress includes per-fold season/stack rows. The two-hour,140 GiB and22-CPU limit is enforced with network and test paths inaccessible. If interrupted, saved task files support resumption under the same frozen registration. Raw outputs stay on the H100; only compact reports/manifests should be fetched to the Mac. Future publication can losslessly compress task chunks with hashes, retaining every record.
