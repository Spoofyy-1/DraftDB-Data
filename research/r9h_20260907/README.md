R9h observed-label training study: the 192-task registration passed independent root review. Runtime authorization is recorded in launch_authorization.json; all code and prepared data are hashed before fitting. Three exact original B references must pass before the new policies run.

The60 prespecified training policies vary start2000/2007/2008, maxcohort Y-1/Y-2, all/historically drafted candidates and observed horizons1..5. Each new target requires every finite ordinal1..h observed by Y-1, sums WAR, clips[-40,40], and recomputes average-tied Gaussian ranks only within admitted training cohorts. Unknown labels are never zero-filled. Every fold needs40 training rows and every admitted cohort needs5 rows.

The same44 B source fields are ordered by stable sorted predictor names for all new fits, replacing the old target-derived order. SHA256(pid) defines row ordering only; PID, cohort, draft pick and outcomes are excluded as predictors. Three fixed model profiles are B TabICL32, E TabICL16/outlier0.5, and F CatBoost MAE/depth7/lr0.1/L2=1/bag1/NaNMax with200 trees. Seeds0/101/202 are fixed; three original B reference replays retain their original matrices/order/targets separately.

Exact matrix/target/query payloads are deduplicated across all folds before fitting. Observed-truth masks require all ordinal1..k values by2018 and are frozen before new fitting. Every query PID remains in model inference. Both full-query legacy truth scores and fixed observed-mask scores must be reported, each against B predictions on the same population; neither can be selected as the preferred result after scoring. Survivor selection, incomplete candidate membership, retrospective vintages and reused development folds prevent a complete-population benchmark claim.

All66 prepared NPZ files stay remote at/home/ubuntu/nba/handoff/r9h/data. Only code, registration and compact proof are local. Source G package is read-only and all64 original hashes are checked; no copies of its full dataset are added locally.
