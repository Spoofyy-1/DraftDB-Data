# Independent bounded review of the v2 input merger

No observed leakage blocker in the reviewed `work/merge_verified_inputs_v2.py`. No source, model, R8t or output snapshot was modified during this review; no GPU work or outcome scoring occurred. The parent owns execution and the full-source versus cutoff-2018 comparison.

Final check: all three initial integrity recommendations are resolved in merger SHA256 `b39fbef05296b729c47d56d20f97ec2c3cb44e58a57f47d39ff78d668309021b`. The exact feature lists are enforced at lines 10–14 and 63; the v1 manifest, exact nine filenames, file hashes, row counts and cohort ranges are enforced at 78–87; raw-token append and written-cell replay are implemented at 101–116. Independent read-only comparison of the existing outputs verified all 1,269,975 original cells across the nine CSVs retain exactly the same text. Full-source and source-cutoff-2018 training CSVs are byte-identical, SHA256 `83f2923ad06f98f34f167f72772c74303a05ad2cd28ad657e208f16db468dfd6`. The parent regenerated the differential-proof record to this current hash. No merger-specific issue remains open.

The source partitions are explicitly 2007–2014, 2015–2018 and 2019–2026 (lines 41–50 in the reviewed version). Each is date-bounded before concatenation; unique player identifiers across the concatenated family are required. The merge uses both player identifier and draft cohort, performs a one-to-one left join and checks original identities/order and parsed base values afterward (55–74). It has no fitting, normalization, imputation, feature selection or cross-cohort aggregation. Future-cohort facts therefore cannot supply training-row values through this join. Unmatched players remain missing. The original training population has 1,458 rows from 2000–2018; the eight inference snapshots each contain their named cohort, totaling 2,419 rows across all nine files.

Independent current-file checks passed all nine v1 input hashes against their existing manifest. The six appended source CSV hashes, row uniqueness, year ranges and family widths passed: original consensus 402 rows, extended consensus 212 training plus 388 inference; original biography 374, extended biography 212 plus 388. All public-package files also matched their allowlists: 54 original consensus, 80 extended consensus, ten original biography and 23 extended biography files. No `y_`, actual-pick or actual-round columns appeared in the nine input headers.

Source-date checks passed for all 16 eligible original consensus snapshots and all 18 eligible extension snapshots: archived availability precedes the draft-day cutoff and the page update date precedes the draft date. The original and extended biography builders recheck the same dated source boundaries. Extended consensus aggregation groups only by player and draft year; its four values are the stated within-player mock-rank summaries. Extended biography selects a whole source row by fixed publisher priority (`verified_mock_bio_extension/build.py:248–255`), without rank, completeness, future classes, outcomes or cross-source field filling. Thus there is no future-fitted derived transform in these new families. This remains evidence about the appended source families, not certification of inherited predictors or the original prospect universe.

The initial review identified the following three integrity issues; each is now resolved as described above. Line references in this historical list refer to the earlier version:

1. At `merge_verified_inputs_v2.py:59–61`, assert the v1 manifest's exact filenames, each input hash, row count and cohort range before joining. The current code records the observed hashes later but does not enforce them. The present files are valid; this would reject subsequent drift.
2. At line 75, preserve original CSV cell tokens during output, or explicitly qualify the preservation claim. Pandas read/write/read alone changes two inherited numeric cells in the current inputs: one training cell by approximately 6.31e-30 and one 2020 cell by approximately 6.16e-33. These are negligible arithmetic differences and no leakage, but the in-memory equality assertion precedes serialization and does not establish exact stored-value preservation. A raw-token append avoids this problem. Full versus cutoff-2018 output equality alone would not catch identical reserialization changes in both outputs.
3. At lines 51 and 66, require the exact registered four `vcons_` and ten `vmb_` names, not only their prefixes and count. The current source files have the correct names and hashes. Exact names strengthen the boundary against a later schema replacement disguised under the same prefix.

The parent's differential check now passes: `train_inputs.csv` is byte-identical when the merger loads all source cohorts versus only source cohorts through 2018. No inference-era source value is needed for any training addition. Original cell text, missingness and row order are unchanged. The output correctly retains its “partially audited, not an approved model matrix or clean benchmark” status because the remaining inherited predictors and original prospect universe remain outside this merger review.
