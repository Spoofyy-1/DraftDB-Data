R9e is a preregistered pre-2019 training-target distribution and model-settings diagnostic. The fixed B44 input matrices, feature order, original sample universe, folds, calendar labels, query WAR truth and canonical row/tie policy are unchanged. Only the eight explicitly registered training-yy transforms and clipping/ensemble settings vary. No query truth is an input to target transforms or effective-design registration.

The 432 requested configurations comprise two normalizers, nine clipping thresholds, three estimator counts and eight deterministic target transformations, each at all three fixed model seeds. Exact CPU preprocessing, target and ensemble equality across every fold/seed may deduplicate before fits; requested64 caps at44 effective estimators. All18 registered D identity references must reproduce raw/canonical predictions, scores and complete audits before interpretation. Hashes of original/transformed targets and nondecreasing/equal-tie invariants are preserved. Clipping may merge unequal targets; original ties cannot split.

All3-seed averages are fixed equal-weight predictions saved separately; no favorable seed selection. Reused-development, missing-label and retrospective-source limitations remain. No new sources, heldout access, promotion or source certification. Immutable raw records stay remote; only compact proofs are fetched locally.

Pre-score validity gate: installed TabICL clipping may remove every training value of a transformed predictor at small thresholds, yielding NaN clipping bounds. Any requested configuration with a non-finite CPU payload in any registered fold/seed is excluded in full before fitting; exact settings, contexts, counts and affected columns are recorded in capacity_check.json and plan.json. No source value or imputation behavior is changed.

Final pre-fit registration retains384 distinct configurations and1152 fits. All48 power-normalization configurations at thresholds0.5/0.75 are excluded because slot_040 receives non-finite clipping bounds in at least one development fold. All18 exact D references remain. No other effective designs were deduplicated.

Identifier amendment after the first launch: decimal-point task IDs violated the unchanged scheduler filename contract. Only ID encoding changes (0.5 becomes0p5). The original frozen registration and runtime failure evidence are preserved; all16 persisted exact reference records remain byte-identical and are resumed without refitting. Tests compare every requested scientific setting and all CPU payloads before/after encoding. Unsaved in-flight work is rerun, with no saved result discarded.
