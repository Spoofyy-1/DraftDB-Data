R9b closes one bounded five-input removal question from completed R9a. The frozen selection rule takes every column whose single-deletion-minus-full and matched-permutation-minus-full means are strictly positive in every development fold and every model seed. Exactly five qualify: college free-throw attempts, made free throws, free-throw rate, made midrange shots and minutes share. The plan records exact column names and hashes of the complete A summary and verification. No Y, Z, confirmation or held-out feedback selects this list.

The 330 registered fits comprise 48 exact A replays (full model, five single removals and ten double removals, each at three seeds), 48 new deletion fits (all 16 subsets of sizes three through five, each at three seeds), and 234 joint-permutation fits (all 26 subsets of sizes two through five, each at three fixed permutation seeds and three model seeds). There is no further exhaustive pruning after this queue without new data or confirmation.

The full X pair06 model, TabICL32 constructor, checkpoint, precision, normalization, historical inputs/labels, training-only preparation and per-fold feature ordering remain unchanged. Deletion occurs only after the full 47-column design has been prepared. It leaves 42–44 columns; width and representation effects are part of the result. Joint controls keep width 47 and move whole selected vectors within training/query role, draft cohort and each player’s exact joint missingness pattern. They preserve within-subset covariance/algebra and cohort vector distributions. They can break dependence with other predictors; sparse strata can be immovable. Shuffle diagnostics are not significance tests. No automatic promotion follows these results.

All 48 full raw/canonical predictions, scores and audit references must match before interpretation. CPU preflight checks all reference input designs and all 94 new designs in every fold. Local fixtures independently count joint vector distributions by cohort/mask and reject an alteration that preserves individual marginals while destroying joint structure. Runtime preserves canonical SHA256(pid) row ordering and exact identical-input ties. IDs and actual draft picks never become predictors. No 2019+ inputs, confirmation, vault or network is mounted. Original retrospective source-vintage, denominator and prospect-universe limitations remain.

The verified four-worker scheduler and immutable append-log task storage are reused, with limits of two hours, 140 GiB and 22 CPU. No weight reuse or model approximation is added. All raw outputs stay on the server; only compact proofs and summaries should be fetched locally. Parent publication can preserve complete records using deterministic lossless chunks. Plan, code, source package and all references are frozen before the first fit. The initial prepare.py package builder may be rerun only before freezing.
