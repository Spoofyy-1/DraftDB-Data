R9j is a fixed-weight stack study on the three unchanged H observed-label payloads. It does not retest2019–2025 or restore the original broader EVOgen11 model.

The48 queue tasks comprise12 exact original B/matched H references,18 XGBoost GPU tasks (depth3/5,600trees),9 deterministic CPU Ridge tasks (alpha30/300/3000), and9 CatBoost GPU tasks (fixed MAE depth7,600trees,200/600 checkpoints). Each task covers the same three development folds. No query rows or query outcome labels enter any member fit, preprocessing fit, stopping decision or feature selector. Native tree NaNs and training-only Ridge median imputation, missingness indicators and scaling are explicit. Both full-query and observed-target-subset scores are always retained.

The35-point four-family simplex with0.25 increments, equal-three-family weights, a fixed80%TabICL/20%CatBoost control and the adapted original rich/thin architecture yield187 distinct recipes after unused zero-weight member settings are deduplicated. Across three payloads and the three fixed seeds plus fixed family-seed ensembles,2,244 prediction recipes are saved losslessly. Primary ranking is full-pool three-fold mean across all three model seeds. Grid maxima are development-search diagnostics, not unbiased validation, an observed-subset goal, or automatic promotion. No metaweights are fitted: later validation outcomes are not falsely treated as contemporaneously known inner-fold truth.

The coverage architecture uses missingness of these44 predictors, with a strictly-below-class-median thin rule and original family weights. It is labeled adapted because original EVOgen11 had broader inputs, distinct residual/quantile components and a different coverage construction. Equal3/equal4 and all adapted-coverage controls remain visible independently of the top10 stack leaderboard.

All model/source manifests, runtime modules, code and references are frozen before fits. CPU preflight checks all nine H payload matrices and targets, the12 saved exact-reference wrappers, a tiny synthetic Ridge preprocessing fixture, constructor arguments, checkpoint bounds and duplicate-score arithmetic. Reference GPU replays must pass before new fits. Every member prediction remains in immutable task files and the append log; every stack prediction receives a deterministic gzip recipe record and round-trip proof.

J requires root review/authorization and waits for I's336 tasks,12 exact references, completed saved blends and successful unit exit. A separate controller may perform only the explicitly authorized single runtime-only restart of I after its original timeout, preserving frozen code/data and all existing task/log bytes. It cannot retry other failures. J runs at most two hours with four workers,140GiB RAM and22 CPU equivalents. This directory has no test/vault mount, scorer or new data collector.

The registered simplex includes pure single-family endpoints. The highest overall recipe must therefore be labeled a standalone control if it has only one positive-weight family; it is not evidence of an ensemble improvement. Completion reporting should additionally identify the best actual ensemble with at least two positive-weight families (the conditional coverage architecture qualifies), while retaining the unchanged full-pool ranking and all fixed references. This distinction changes reporting only, not any recipe or model selection during fitting.
