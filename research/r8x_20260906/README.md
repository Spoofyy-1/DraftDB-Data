# R8x pair complement screen

The frozen parent registration selects all five W fields that exceeded the fixed45-column baseline and had positive matched gains in every fold mean and every seed mean. All ten unordered pairs are tested, with no further selection. There are318 fits:3 baseline and15 original single-feature replays, then30 fits per pair (RR3 and RP/PR/PP9 each). All data, calendar cutoffs, rank targets, source-only41 + dated-consensus4 backbone, TabICL32 settings and canonical identity/exact-vector tie policy are unchanged. No2019+ inputs, test answers or confirmation folds are mounted.

Singles retain W slot041. Pair features are alphabetically assigned to slots041/042, giving47 columns in every real/control pair arm. RP and PR reuse W's fixed single-field shuffle stream within cohort, role and the field's missingness pattern. PP shuffles whole two-field vectors within cohort, role and joint missingness, preserving joint structure. Missingness and cohort marginals remain exact. Conditional shuffles can break cross-feature relationships; these are exploratory usability comparisons, not significance tests or a factorial independent-shuffle test.

All18 references must exactly reproduce W configurations, raw/canonical predictions, scores and full audits before any pair result is interpreted. Each pair reports RR−PR (A given B), RR−RP (B given A), RR−PP, fold/seed variation, and actual RR minus baseline and both original singles. The registered exploratory candidate rule requires both positive mean conditional effects, both positive in every fold mean, and RR exceeding both singles. No candidate is automatically promoted; all ten pairs and failures are retained.

The worker delegates the frozen backbone and reference fits to unchanged W code. Pair code only appends the two audited columns. Source hashes and train-only eligibility remain enforced. Source vintage, input denominator disagreements and original prospect-universe limitations remain; no full-source certification is claimed. The metric is pre-2019 mean-fold Spearman, not classification accuracy.

The scheduler is copied unchanged from the verified72-replay performance benchmark. Four workers refill immediately; complete raw outputs persist as immutable tasks/*.json plus an fsynced append log. Compact progress updates every3 seconds and full summaries every20 seconds/final. A separate snapshot callback adds only minimal per-fold season/stack values for the dashboard. Failed or partial corrupt logs fail closed; files and registered initial records are pinned. This avoids repeatedly rewriting the full nested audit state while retaining every raw result.

Local controls/tamper fixtures do not fit models or create training data. Isolated server CPU preparation verifies W source, label, identity, column and reference-design hashes plus all pair RR/PP widths and registered constructors. Frozen hashes cover worker, plan, reference records, source manifests, scheduler, runner and tests before models start. The independent parent worker/plan review found no scientific blocker. The run is bounded to4 workers,140 GiB,22 CPU-equivalents and2 hours, with network access disabled and only results writable.
