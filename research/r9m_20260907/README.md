R9M keeps L's exact TabICL32/outlier2, Ridge30/300/3000 preprocessing and600-tree CatBoost settings on the same observed-ordinal1 H training policy, real season cutoffs and query population. The four learner/runtime files are byte-identical to L. The eight panels are44,92,129,112,107,149,127 and164 columns: original44; F50; F50 with combine, team, game, combine+team, game+team, or combine+game+team. No dated-bio family is added.

There are24 task records: six original L control records reused unchanged and18 new model jobs. This requires162 new fits;54 historical control fits are reused, never reported as new work. Before any candidate fit, the root-only reuse gate verifies the original completed-log record hashes, frozen source registration, original/current input and label hashes, complete query PID/order, exact member raw/canonical predictions, model parameters, runtime pins and identical-input tie audits. Source records retain their original L protocol and input-manifest hashes. M bookkeeping is stored beside them. CatBoost is not refitted to manufacture bit-exact GPU reproducibility.

Each panel retains the38 fixed same-panel recipes. A separate cross-panel grid lets TabICL use F50 or one of the six candidate panels and lets Ridge/CatBoost independently use44 or that candidate. The1,824 view/weight designs deduplicate by positive-weight member/view tuples to950 distinct recipes before fitting. They produce11,400 saved prediction records across three folds and four aggregation modes without extra fits or learned weights. Same-seed metrics and J's fixed-three-seed family-rank-average mode remain separate. Final already-tied rank scores are validated without re-averaging. Actual model-family ensembles remain distinct from standalone or multiple-view controls.

Full-query development score is primary; the frozen complete-target mask is always secondary. Family-width effects, retrospective source vintages, incomplete population and repeated development search remain limitations. No2019+ inputs, outcomes, scoring, promotion or claimed goal result are included. Root-only scoring preserves same-panel summaries separately from cross_panel_summaries. The legacy API key stack_summaries_oof represents fixed-weight recipes here, not learned OOF weights.

Preparation commands are CPU-only: deploy learner files into the remote code directory; run build_inputs.py once to create exact per-job inputs and separate scoring/source references; run cpu_preflight.py. The builder never copies private data to this Mac. Root owns review, freeze, immutable launch authorization and the bounded four-worker/two-hour/140GiB/22CPU launch. The sandbox mounts only one job's training/inference/manifest bundle. No service is launched by preparation scripts.

Twelve local fixtures pass, including all950 recipes and11,400 finite saved vectors, all1,824 design aliases, manual integer-rank and fixed-three-seed comparisons, no-extra-fit accounting, PID/target/prediction tampering, source-file/column continuity, unknown-label/cutoff rejection and atomic interrupted-output preservation. A partially appended completion-log tail fails closed and requires explicit recovery; already published immutable worker records can be recovered on resume.
