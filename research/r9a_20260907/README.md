R9a tests the existing 47 predictors of the unchanged X usage-slope plus posterior-rim model. Columns are enumerated from schema only; no Z, Y, confirmation or held-out scores select fields or model settings.

The registration contains 3,810 fits: three exact full-model X references, 47 single removals × three model seeds, all 1,081 double removals × three seeds, and 47 single-column permutation controls × three permutation seeds × three model seeds. The model remains TabICL32, with the original checkpoint, normalization, precision, seeds and training-only preprocessing. Four workers use the independently verified immediate-refill queue, with a two-hour, 140 GiB and 22 CPU limit. There is no weight reuse.

Each fold first prepares the entire unchanged X design, including its original training-selected column order. Deletion happens last, retaining all other column names, values, order and row indices. The same 47 semantic predictors occur in all three folds, while their existing order differs. The plan maps consensus and F50 slot names to actual source fields. No selector is rerun after deletion. Permutation controls keep all 47 columns and shuffle only the specified column within training/query role, draft cohort and its own exact missingness mask. Their fixed stream names use the existing helper namespace `fifty:registered_input:<column>`; this is only a random-stream identifier and never a predictor or source-family claim.

The original calendar-label rule, pre-2019 development folds (2012–2014), canonical SHA256(pid) row order and exact-identical-input tie averaging remain unchanged. IDs never enter the predictors. No 2019+ inputs, vault, confirmation outcomes or network are mounted. The original source-only college and dated-consensus package is read-only. Retrospective source vintage, denominator quality and prospect-universe limitations remain; this study does not certify those sources.

Before interpretation, all three full-model runs must exactly match X raw predictions, canonical predictions, scores and complete input audits. CPU preflight replays all baseline/source/label/pid/column hashes, all 47 single removals, three representative double removals and all 141 permutation designs in every fold. Essential fixtures check schema completeness, reference and future-label tamper rejection, unchanged predictors, missing masks, cohort marginals and shuffle reproducibility. The requested constructor is checked directly; there is no fallback. Plan, code, sources and full references are frozen before the first fit.

Deletion changes model width from 47 to 46 or 45. Its effect can include dimension and representation changes and is not a pure causal information test. Fixed-width shuffles preserve marginal/cohort/missingness distributions but may break algebra and dependence with other predictors. Summaries retain paired model-seed and fold changes versus the full model; three permutation seeds are exploratory controls rather than independent statistical observations. No significance claim, automatic promotion or held-out evaluation follows this queue.

All complete task JSON records remain immutable on the server in `results/tasks/`, with SHA-256 append-log verification and resumable persistence. Compact dashboard records retain per-fold scores. Because local disk is limited, fetch only compact status, summary and verification artifacts; future publication can use deterministic lossless task chunks. Corrupt partial append-log tails fail closed and need explicit repair before resume. `prepare.py` is the initial schema/package builder; the final frozen implementation and registration are authoritative.
