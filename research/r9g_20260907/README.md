R9g freezes576 XGBoost GPU configurations x3 seeds plus3 exact B TabICL references (1,731 tasks). It is independent of F scores. All fits use the unchanged44 B predictors and original yy. Experimental training rows are sorted by cohort then SHA256(pid), with qid only in the training DMatrix group metadata. Query rows retain original canonical order and exact identical-vector ties. No identity, cohort, actual pick or outcome enters as a predictor.

The complete declarative grid is registered before models. Each fit runs1,200 boosting rounds without evaluation sets or early stopping and saves fixed200/600/1,200 checkpoints. Original negative targets remain unchanged for both squared-error and pairwise objectives. GPU device and effective objective/configuration are checked; no CPU fallback. Native NaNs are used without imputation. B/E anchors, fixed three seeds and all alpha0..1 step.1 rank blends match the F diagnostic protocol; all saved, no promotion.

CPU fixtures pin sources, original and reordered matrix/label/PID hashes, cohort group sizes, order invariance, task IDs, iteration boundaries, query exclusion and tie behavior. Two two-tree GPU compatibility probes run only after F completes successfully, within the G unit and after registration freeze. They use original pre2019 training data and compute no scores. The separate sequential watcher requires F435 tasks, all3 exact references, saved blends complete and successful unit exit. No F/G oversubscription.

All raw records stay remote. Four workers,140GiB,22CPU,2h per model batch. Reused-development/source-vintage/missing-label limitations remain. This is exploratory model comparison, not benchmark measurement or significance testing.
