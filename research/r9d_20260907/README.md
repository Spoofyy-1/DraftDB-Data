R9d changes only TabICL normalization, outlier threshold and requested estimator count on the frozen B drop5_001 baseline (44 predictors). The same source inputs, calendar labels, training-only preparation, checkpoint, precision, canonical row order and exact-input ties remain in force. No C data, further feature pruning, test or confirmation inputs are included.

The requested factorial is all five installed named normalizers (none, power, quantile, quantile_rtdl, robust), thresholds 2/3/5/disabled and estimator counts16/32/64:60 configurations and180 seed fits before deduplication. The factory default mixture is not an additional named-method arm. Installed source hashes and CPU payload proofs are saved. Disabled clipping is serialized as the string disabled and becomes positive infinity only in the constructor; fitted infinite bounds prove identity on finite inputs. No nonstandard Infinity JSON is emitted.

CPU preflight mirrors the installed fit preprocessing, including float32 targets, train-only scaling and encoding. It compares complete effective options, ensemble configurations, ordered feature permutations and exact transformed train/query/label views across all three folds and all three seeds. Only demonstrably identical designs are deduplicated, without scores. The requested-to-evaluated map preserves every requested setting. Actual runtime fitted-generator fingerprints and normalized effective constructors must match those proofs. Requested64 can yield44 effective members because the single-normalization permutation bank is limited by feature count; actual counts are recorded.

Three baseline runs must exactly replay B raw/canonical predictions, scores and complete audits before interpretation. Each evaluated configuration reports paired fold/seed changes versus baseline. After all fits, a separate prespecified diagnostic takes the arithmetic mean of all three saved canonical predictions per player, with fixed equal weights; it never selects favorable seeds. Full averaged predictions and their development Spearman are saved separately from single-fit means. No promotion, significance or held-out claim follows this sweep.

All raw tasks remain immutable on the server. The existing four-worker immediate-refill scheduler, two-hour limit,140GiB memory and22CPU quota are retained. No weight reuse or approximation is added. Normalization-related source and generalization limitations from the original diagnostic baseline remain. Plan, code, sources, installed-source hashes, CPU dedupe proofs and references are frozen before the first model fit. The initial prepare/capacity builders must not be rerun after freezing.
