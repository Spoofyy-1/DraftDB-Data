R9N varies only TabICL normalization, clipping and requested ensemble size on the frozen127-input M f50_game_team panel. It preserves the exact observed-ordinal1 H labels, source-season cutoffs, feature/row order, checkpoint, precision and identical-input tie policy. Ridge30/300/3000 and600-tree CatBoost predictions are reused from immutable M base44/G records. No controls are refitted.

The request contains23 profiles: none normalization at clipping thresholds0.5/1/2/4 and ensemble sizes16/32/64/128/256, plus power normalization at threshold2 and64/128/256. The original none/2/32 profile is reused. Before admission this gives66 new jobs and198 new TabICL fits, with three seeds per outer-fold job. Four workers refill immediately. Actual counts are finalized by CPU-only preparation before fitting.

The model-free preparation exactly follows the installed TabICL encoder, float32 training-target standardizer, normalization, clipping and ensemble generation. All prepared training/query model tensors must be finite. Unsupported or nonfinite profiles are explicitly excluded as entire profiles before scores exist; a failed baseline stops preparation. Complete fingerprints include matrix/target/PID/column order, transformations, prepared tensor shapes and dtypes, permutations and effective constructors. Only exact equivalence across all nine fold/seed payloads permits requested-size aliases. Matching estimator counts alone never establishes equivalence. Requested and effective sizes, warnings, aliases and exclusions remain visible. Each actual fit must reproduce its frozen CPU prepared fingerprint.

For every profile, the38 fixed weight recipes combine TabICL on G with Ridge and CatBoost independently on44 or G. Positive-member/view/profile deduplication gives1,723 original distinct recipes from3,496 declared combinations;44 recipes contain no TabICL and are shared across profiles. After any justified CPU profile aliases/exclusions, the active recipe set is deduplicated before fitting. Same-seed scores and the fixed-three-seed family-rank-average mode are reported separately. Weights are never learned. Standalone controls remain distinct from actual multiple-model-family stacks.

The independent source gate verifies all six original M/L records, complete-log hashes, input/label/PID order, model/runtime pins and canonical ties. The completed M result manifest also binds the saved recipe diagnostics. M cross_0771 and G stack_013 must reproduce24 prediction records and both frozen metrics exactly before any candidate fit. Source records retain their original registration and manifest hashes; separate N metadata records reuse. Root-only scoring sees the frozen development truth. Each learner namespace receives only its single job's training.npz, inference.npz and manifest.json, and no scorer or source-record directory.

Full-query development mean across all three folds/seeds is primary; the frozen observed-target mask is secondary. The127-column panel and settings are development selections. Population incompleteness, retrospective source vintage and reused development data remain limitations. This is not a2019+ benchmark evaluation, confirmation, promotion or55% goal claim.

Run `python3 work/r9n/test_n.py -v` for the eight finite-fixture tests. They cover all23 profiles, all1,723 recipes/20,676 finite prediction records, all3,496 design aliases, manual integer-rank and fixed-seed arithmetic, nonfinite rejection, complete nine-payload alias signatures, PID/target/weight tampering and both reference recipes. No predictive models are fitted by these tests. Root owns deployment, the remote-only build_inputs.py preparation, isolated cpu_preflight.py, final freeze and launch authorization. The coordinator is bounded to four workers; root sets the two-hour/140GiB/22CPU service limits. Partial log tails fail closed; durable immutable result files can be recovered without repeating completed fits.
