The latest result is lower on the original metric. I found no scoring-direction, horizon, player-join, feature-column alignment or averaging implementation error in this bounded audit. This does **not** reproduce or invalidate the original model: the new test used a different model, features and training population/target.

| Evaluation | Years | Model score ×100 | Draft comparator ×100 |
|---|---|---:|---:|
| Published original stack, expanding | 2020–2025 | 50.6000 | 24.0167* |
| New H recipe, full legacy benchmark, fixed three-seed rank average | 2020–2025 | 30.9846 | 24.0341 |
| New H recipe, same full benchmark, mean of individual seed correlations | 2020–2025 | 31.1244 | 24.0341 |
| New H recipe, full legacy benchmark, fixed rank average | 2019–2025 | 30.8708 | 26.3455 |
| New H recipe, complete-target subset, fixed rank average | 2019–2025 | 29.2573 | 21.2748 |

*Original means are recomputed from the published three-decimal per-year rows in `site/model/CURRENT_MODEL.json`; all seven new per-year draft comparators equal those original rows at their published precision. That small rounding difference is expected. Original strict 2019–2025 score from the same rounded rows is45.2143%. All figures are Spearman rho×100, not percent of positions correct.

Dropping2019 from the new result increases it by only0.1138 points. The matched-six-year gap is19.6154 points; the year-range difference does not explain it. The difference between rank-averaged predictions and averaging per-seed correlations is also small here. No per-player test truth was reopened: these are arithmetic recomputations from the already saved per-year aggregates.

The original and new **full** scorers both sum the firstk WAR components, substitute zero for missing components, compute `spearmanr(prediction, truth)` and compute the comparator with negative actual pick. Sources: `work/audit/vault.py:85–101`, `work/audit/honest.py:207–215`, `work/r9test/score_frozen.py:135–168`. Original horizons are5/5/5/4/3/2/1 for2019–2025; the frozen new policy uses exactly those horizons. The original scorer permits missing prediction PIDs to be omitted; the new scorer requires exact equality with the original drafted answer PID set before joining. Its successful saved run proves no omission for these predictions. The minimum-correlation sample thresholds differ (old10, new3), which has no effect on these52–58-player full cohorts or32–55-player observed subsets.

Five isolated fixtures covering horizons1–5, negative WAR, known zeros, missing seasons and prediction ties reproduced the old/new arithmetic exactly. The audit extracts only the pure old scoring calculation with AST and never imports `vault.py`, which would open answer files. It also never executes the new scorer's answer-reading function. These fixtures are software tests, not model training data.

The44-slot transfer is internally consistent. `work/r9test_inputs/build.py:113–115,165–173` maps38 context fields identically, then `slot_037..040` to the four dated consensus ranks and `slot_041..042` to career usage slope/posterior rim. `prepare_bundles.py:18–31` reverses that mapping for physical data and `model_core.py:17–30` restores the model columns in the same order. I independently recomputed all seven query matrices from the frozen public inputs and drafted membership metadata: all44 FP64 values, column order, canonical PID order and saved prediction/audit hashes agree. There is no observed accidental actual-pick or PID predictor. This verifies transfer mechanics, not retrospective source vintage or completeness.

The57.9% development number describes2012–2014 **observed-target subsets**,28/55,26/50 and28/53 players, with query horizons5/5/4. It was the mean of nine fold/seed correlations, not the full-class score or the score of a selected best seed. This subset covers82/158 historical query players and favors sufficiently observed careers. The new report preserves both the full benchmark and its separate complete-target subset;29.3% is the latter. Comparing57.9 directly to the old50.6 mixes subsets, years and different model recipes.

The original `CURRENT_MODEL.json` describes a richer XGB/TabICL/Ridge stack:75% Ridge+25% TabICL on rich rows,100% XGB on thin rows; window2007; feature engineering, shrinkage and broader legacy feature families. The new test is standalone TabICL16/outlier0.5 on44 fixed source fields, start2000, historically drafted training rows with exactly two finite observed seasons, with no missing training WAR converted to zero. All nine H development fits replayed its raw/canonical predictions and settings exactly. This is an exact transfer of the requested H recipe, not an exact transfer of the original50.6% incumbent.

The original expanding code preserves the original baseWg unchanged and only censors appended prior test cohorts (`work/audit/evolve3.py:329–345`, `work/audit/vault.py:42–59`). That structural calendar difference is independently visible in code; root is separately auditing its finite-fact impact. It would be incorrect to assign the19.6-point gap to calendar fixes, removed features, sampling or model choice without a matched replay. New2024/2025 fits also use verified training calendar only through2022, an explicit coverage limitation.

`verification.json` records all seven source-to-prediction matrix checks, five pure metric fixtures, matched-year arithmetic, draft comparator agreement and source hashes. No models fitted, no new answers opened, no I results accessed, no recipe tuned, no publication performed.
